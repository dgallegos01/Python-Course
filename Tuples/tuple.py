# tuples are like lists but they cannot be changed or modified
numbers = [1,2,3] # this is a list
numbers = (1,2,3) # this is a tuple
# there are only two methods to tuples:
numbers.count()
numbers.index()
numbers[0] = 10 # this will not work because you cannot modify tuples 